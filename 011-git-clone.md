# `git clone`

[Git Cheat Sheet](./README.md) / git clone

---

1. Clone remote repo

   **_Command_**

   ```none
   git clone <REMOTE_REPO_URL>
   ```

2. Clone remote repo which contains submodule

   **_Command_**

   ```none
   git clone <REMOTE_REPO_URL> --recurse-submodules
   ```
